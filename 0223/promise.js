fetchApi <- web api

new XMLHttpRequest()
$.ajax({
	
}).done(response, callback).fail().complete

const promise = new Promise()
fetchApi(url, 요청헤더에 넣어줘야하는 것들).then()
promise.then()


function async name(){
	await 
}